server {
    listen 443 ssl;
    server_name localhost;

    ssl_certificate /etc/nginx/certs/keystore.crt;
    ssl_certificate_key /etc/nginx/certs/keystore.key;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri /index.html;
    }
}
